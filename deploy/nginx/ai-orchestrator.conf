server {
    listen 80;
    server_name _;

    root /var/www/ai-orchestrator/site;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    access_log /var/log/nginx/ai-orchestrator.access.log;
    error_log  /var/log/nginx/ai-orchestrator.error.log;
}
